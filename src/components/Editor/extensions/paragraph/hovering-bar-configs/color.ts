import ColorText from "../components/ColorText";
import { IConfigItem } from "@/components/Editor/types";

const colorConfigs: IConfigItem[] = [
  {
    id: "color",
    order: 10,
    element: ColorText,
  },
];

export default colorConfigs;
