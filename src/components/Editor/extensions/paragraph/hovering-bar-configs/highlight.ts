import HighlightText from "../components/HighlightText";
import { IConfigItem } from "@/components/Editor/types";

const highlightConfigs: IConfigItem[] = [
  {
    id: "highlight",
    order: 11,
    element: HighlightText,
  },
];

export default highlightConfigs;
